# YarnHooker
A simple mod that hooks `SetNode` to allow loading nodes from a *.yarnc file (if available). Primarily designed for NONMP.
*.yarnc file location can be set in `UserData/yarn.cfg`

[See also](https://github.com/IruzzArcana/YarnLocalHooker)
